[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
via_ir = true
evm_version = "shanghai"

# Ritual Chain (chain id 1979). RPC is public; no secret here.
[rpc_endpoints]
ritual = "https://rpc.ritualfoundation.org"
